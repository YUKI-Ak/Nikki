<h1>top page</h1>

<%= link_to '記事の作成', new_article_path %>

<% @articles.each do |article| %>
  <p>
    <strong>Title:</strong>
    <%= link_to article.title, article_path(article) %>
  </p>
<% end %>